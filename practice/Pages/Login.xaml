<Page x:Class="practice.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:practice.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="505" d:DesignWidth="404"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      Title="Login"
      >
    <Page.Resources>
        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
        <Style x:Key="TextBoxName" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Margin" Value="50,0,0,0" />
        </Style>
        <Style x:Key="ErrorLabel" TargetType="Label">
            <Setter Property="Foreground" Value="#cf1322" />
            <Setter Property="FontSize" Value="16" />
        </Style>
    </Page.Resources>
    <ui:SimpleStackPanel>
        <Frame Name="MainFrame" />
        <ui:SimpleStackPanel Margin="0,30,0,10" >
            <TextBlock Text="{DynamicResource login_header}" Style="{StaticResource Header}"/>
            <Label x:Name="status" Content="plug" Visibility="Hidden" HorizontalAlignment="Center" Margin="0,10,0,0" Style="{StaticResource ErrorLabel}"/>
        </ui:SimpleStackPanel>
        <Grid>
            <TextBlock Text="{DynamicResource login_username}"  Style="{StaticResource TextBoxName}"/>
            <TextBox x:Name="loginTextBox" Margin="50,25,50,0"/>
        </Grid>
        <Grid Margin="0,30,0,30">
            <TextBlock Text="{DynamicResource login_password}"  Style="{StaticResource TextBoxName}"/>
            <PasswordBox x:Name="passwordTextBox" Margin="50,25,50,0"  />
        </Grid>
        <Grid Margin="0,40,0,10" HorizontalAlignment="Center">
            <Button Click="Button_Click" Content="{DynamicResource login_button_text}" MinWidth="180" MinHeight="40" />
        </Grid>
        <Grid HorizontalAlignment="Center">
            <TextBlock>
               <Run Text="{DynamicResource login_no_account_0}"/>
               <Hyperlink Click="Hyperlink_Click"><Run Text="{DynamicResource login_no_account_1}" /></Hyperlink>
            </TextBlock>
        </Grid>
    </ui:SimpleStackPanel>
</Page>
